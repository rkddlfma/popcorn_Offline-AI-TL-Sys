# 번역 백엔드 선택: transformers | vllm
TRANSLATE_BACKEND=vllm

# VLLM 서버 주소 (WSL에서 실행 중인 경우 host.docker.internal 사용)
VLLM_URL=http://host.docker.internal:8001

# 번역 모델
TRANSLATE_MODEL=google/translategemma-4b-it

# Whisper 모델 크기: tiny | base | small | medium | large
WHISPER_MODEL=base
